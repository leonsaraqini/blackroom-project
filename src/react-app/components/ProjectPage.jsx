import LegacyRoutePage from './LegacyRoutePage.jsx'

export default function ProjectPage({ source, pathname, title }) {
  return <LegacyRoutePage source={source} pathname={pathname} title={title} />
}
